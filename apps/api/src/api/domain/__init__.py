"""Domain layer.

Contains entities, value objects, business rules, and the interfaces
(ports) that infrastructure implements.

This package must never import a framework, driver, or web library -- see
the `Domain is framework-free` contract in the root `pyproject.toml`.
"""
