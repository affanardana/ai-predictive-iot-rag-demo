"""Application layer.

Use cases that orchestrate domain objects through the ports defined by the
domain. This layer coordinates; it does not compute business rules, execute
SQL, parse HTTP, or load models.
"""
