[project]
name = "api"
version = "0.2.0"
description = "FastAPI backend for the AI Predictive Maintenance platform"
requires-python = ">=3.12,<3.14"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pydantic>=2.9",
    "pydantic-settings>=2.6",
    "sqlalchemy[asyncio]>=2.0.36",
    "psycopg[binary]>=3.2",
    "alembic>=1.14",
    # The `vector` column type for retrieval (PRD section 25 makes
    # PostgreSQL/pgvector mandatory). This is the Python side only -- it emits
    # `vector(384)` and the `<=>` operator; the extension itself is installed by
    # migration 0003, because a column type that exists in the model but not in
    # the database fails at the first query rather than at import.
    "pgvector>=0.3.6",
    # A runtime dependency, not a test one. `HttpPredictor` and the composition
    # root both import it, so an install of `api` without the workspace's dev
    # group fails at `build_container` with `ModuleNotFoundError`. It went
    # unnoticed because CI installs every group; it surfaced when the API was
    # packaged for deployment and nothing else was there.
    "httpx>=0.28",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/api"]
